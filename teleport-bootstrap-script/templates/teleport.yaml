ssh_service:
  enabled: yes
  listen_addr: 0.0.0.0:3022

  commands:
    - name: arch
      command: [uname, -p]
      period: 1h0m0s
  permit_user_env: false
auth_service:
  enabled: no
proxy_service:
  enabled: no
